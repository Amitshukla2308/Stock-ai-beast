services:
  atlas-learn:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    env_file:
      - .env
    command: python -m engine.gateway learn --days 1825 --symbol NIFTY
    network_mode: host
